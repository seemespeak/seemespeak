- if entries.from > 0
  %a{href: videos_path(params.merge(from: entries.from - 20))}
    <

Entries #{entries.from + 1} to #{entries.from + 20} of #{entries.total}

- if (entries.from + entries.count) < entries.total
  %a{href: videos_path(params.merge(from: entries.from + 20))}
    >
